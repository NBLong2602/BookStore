<nav class="navbar navbar-expand-lg bg-light navbar-light py-3 py-lg-0 px-0">
    <a href="" class="text-decoration-none d-block d-lg-none">
        <h1 class="m-0 display-5 font-weight-semi-bold"><span class="text-primary font-weight-bold border px-3 mr-1">Book</span>Store</h1>
    </a>
    <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-between" id="navbarCollapse">
        <div class="navbar-nav mr-auto py-0">
            <a asp-controller="Home" asp-action="Index" class="nav-item nav-link">Home</a>
            <a asp-controller="Product" asp-action="AllProduct" class="nav-item nav-link">Shop</a>
            <a href="" class="nav-item nav-link">Shop Detail</a>
            <div class="nav-item dropdown">
                <a href="" class="nav-link dropdown-toggle" data-toggle="dropdown">Pages</a>
                <div class="dropdown-menu rounded-0 m-0">
                    <a asp-controller="Cart" asp-action="Index" class="dropdown-item">Shopping Cart</a>
                    <a asp-controller="Cart" asp-action="Checkout" class="dropdown-item">Checkout</a>
                </div>
            </div>
            <a href="" class="nav-item nav-link">Contact</a>
        </div>
        <div class="navbar-nav ml-auto py-0">
            @using Microsoft.AspNetCore.Http
            @{
                var nameUser = Context.Session.GetString("UserName");
                if (nameUser != null)
                {
                    <a asp-controller="User" asp-action="Purchase" class="nav-item nav-link">Hi @nameUser </a>
                }
                else
                {
                    <a asp-controller="Account" asp-action="Login" class="nav-item nav-link">Login</a>
                }
            }
            <a asp-controller="Account" asp-action="Logout" class="nav-item nav-link">Logout</a>
        </div>
    </div>
</nav>